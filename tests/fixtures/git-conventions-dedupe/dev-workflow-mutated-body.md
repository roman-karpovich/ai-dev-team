# Developer Workflow (shared mutated fixture)

## Git Workflow

- **Feature branch** — placeholder text.
- **Base branch detection** — placeholder text.

### Pre-commit branch assertion (MANDATORY)

Before every commit, do whatever feels right. Rules deliberately omitted to
exercise the body-pin assertions in `check_dev_workflow_git_canonical`.

### Post-merge bug flow

A placeholder post-merge paragraph that omits the load-bearing case wording
and the final enforcement sentence.
