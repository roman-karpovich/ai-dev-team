# Example spec

## 1. Context

Some context prose.

## 1.1 Attack-surface profile

This file uses CRLF line terminators. str.splitlines() normalization must
strip the stray carriage return so the $-anchored section-ere still matches.

## 2. Current State

Current state prose.
