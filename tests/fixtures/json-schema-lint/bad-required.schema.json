{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "X8/X10 regression fixture — 'required' is a string, not a list. A string is iterated character-by-character by validate(), silently misbehaving. The schema-walk value-type check must reject it with exit 2.",
  "type": "object",
  "required": "name",
  "properties": {
    "name": { "type": "string" }
  }
}
