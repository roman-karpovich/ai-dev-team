{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "X8/X10 regression fixture — 'enum' is an empty list. An empty enum can never match any instance value; the schema-walk requires a non-empty list and must reject it with exit 2.",
  "type": "object",
  "properties": {
    "name": { "type": "string", "enum": [] }
  }
}
