{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "X8 regression fixture — additionalProperties is the string \"false\", not a boolean. The gate in validate() fires only on `is False`, so a non-boolean value would silently disable the extra-property gate. The schema-walk value-type check must reject it with exit 2.",
  "type": "object",
  "additionalProperties": "false",
  "properties": {
    "name": { "type": "string" }
  }
}
