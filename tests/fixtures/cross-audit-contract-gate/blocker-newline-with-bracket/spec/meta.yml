expected_classification: BLOCKER_YAML_UNSAFE_NEWLINE
expected_exit: 1
mode: spec
notes: "synthetic; X10 inverse — genuine physical-newline-split blocker whose first line contains a literal '[' inside the scalar must still be detected as unsafe_newline (not balanced early)"
