# C2 Pointer Inside a Fence — not a rendered pointer

A section pointer printed INSIDE a fenced block is documentation OF the
syntax, not a live pointer — Obsidian renders it as code. Zero C2:

```text
`Missing.md` §Nope
```
