---
title: Terminal spec, both evidence keys valid (nonfire-both-valid)
project: fixture
type: spec
status: VERIFIED
spec_audit_evidence: dual_model
code_audit_evidence: single_model
created: 2026-07-01
tags: [spec, fixture]
---

# Terminal spec, both evidence keys valid

VERIFIED + post-cutoff with BOTH keys present and enum-valid (a degraded but
honest `single_model` still counts — the gate targets absence/null/off-enum, not
the `dual_model` grade) → no C8.
