---
title: Terminal spec, created absent (nonfire-created-absent)
project: fixture
type: spec
status: SHIPPED
tags: [spec, fixture]
---

# Terminal spec, created absent

SHIPPED with BOTH evidence keys absent, but NO `created:` key — the cutoff gate
cannot be evaluated → skip (conservative FN) → no C8.
