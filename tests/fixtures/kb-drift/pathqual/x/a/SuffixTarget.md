# Suffix Target

A note nested at `x/a/SuffixTarget.md`. The CLEAN path-qualified witnesses
(`[[a/SuffixTarget]]`, slash-normalized variants) resolve here via Obsidian
component-suffix matching: `("a","suffixtarget")` is a tuple-suffix of
`("x","a","suffixtarget")`.
