# Boundary Target

A note at `x/a/BoundaryTarget.md`. The BROKEN boundary witness
`[[xa/BoundaryTarget]]` must flag C1: `("xa","boundarytarget")` is NOT a
tuple-suffix of `("x","a","boundarytarget")` (component-boundary, not string
boundary) — string suffixing would wrongly match `x/a`.
