# FN-closer witness (BROKEN — 1 C1)

A genuinely-broken path-qualified wikilink whose bare stem exists ELSEWHERE:
[[wrong/path/UniquePathTarget]] does not suffix-match `right/path/UniquePathTarget.md`
→ exactly 1 C1. The old unconditional bare-stem fallback false-NEGATIVE'd this.
