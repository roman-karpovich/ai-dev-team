# Component-boundary witness (BROKEN — 1 C1)

A wrong path-qualified wikilink whose components do not respect the path
boundary: [[xa/BoundaryTarget]] does NOT suffix-match `x/a/BoundaryTarget.md`
(`xa` != `x`/`a`) → exactly 1 C1. Tuple (not string) suffixing guarantees this.
